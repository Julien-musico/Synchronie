# Variables d'environnement pour Render.com
# À configurer dans le dashboard Render

# Configuration Flask
SECRET_KEY=your-super-secret-key-for-production
FLASK_ENV=production

# Base de données (fournie automatiquement par Render PostgreSQL)
# DATABASE_URL sera automatiquement fournie par Render

# API Keys IA (à configurer avec vos vraies clés)
OPENAI_API_KEY=your-openai-api-key
MISTRAL_API_KEY=your-mistral-api-key

# Configuration Admin par défaut
ADMIN_EMAIL=admin@synchronie.fr
ADMIN_PASSWORD=ChangeThisPassword123!

# Configuration Email (optionnel)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Redis pour Celery (optionnel, peut être ajouté plus tard)
# CELERY_BROKER_URL sera fournie par Render Redis si ajouté
# CELERY_RESULT_BACKEND sera fournie par Render Redis si ajouté
